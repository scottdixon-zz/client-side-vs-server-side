<%= form_with(model: grocery, local: true) do |form| %>
  <% if grocery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grocery.errors.count, "error") %> prohibited this grocery from being saved:</h2>

      <ul>
      <% grocery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_field :item, id: :grocery_item %>
  </div>

  <div class="field">
    <%= form.label :qty %>
    <%= form.number_field :qty, id: :grocery_qty %>
  </div>

  <div class="field">
    <%= form.label :purchased %>
    <%= form.check_box :purchased, id: :grocery_purchased %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
